name: brother_label_printer_example
description: Demonstrates how to use the brother_label_printer plugin.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=2.10.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  brother_label_printer:
    path: ../

flutter:
  uses-material-design: true